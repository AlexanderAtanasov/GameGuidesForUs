logging:
  level:
    blog: WARN
    org: WARN
spring:
  datasource:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    url: jdbc:hsqldb:mem:testdb; sql.syntax_mys=trueb:mem:testdb; sql.syntax_mys =true
  jpa:
    hibernate:
      ddl-auto: create
      database-platform: org.hibernate.dialect.HSQLDialect
  #    open-in-view: false
  #    properties:
  #      hibernate:
  #        format_sql: DEBUG
  #      defer-datasource-initialization: true
  #  mvc:
  #    hiddenmethod:
  #      filter:
  #        enabled: true
  #  servlet:
  #    multipart:
  #      max-file-size: 10MB
  #      max-request-size: 10MB
  sql:
    init:
      mode: never

##Cloudinary Properties
#cloudinary:
#  api-key: 313879687618881
#  api-secret: ${CLOUDINARY_API_SECRET}
#  cloud-name: alexat92